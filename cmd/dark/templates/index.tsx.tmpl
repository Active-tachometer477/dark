{{if eq .UILib "react"}}import React from 'react';{{else}}import { h } from 'preact';{{end}}

export default function IndexPage({ title, message }) {
  return (
    <div>
      <h2>{title}</h2>
      <p>{message}</p>
    </div>
  );
}
